# Private-storage allowlist — file-creating calls under TelegramConcierge/ that do
# NOT go through PrivateStorage. Format: <path> :: <trimmed source line> :: <reason>.
# A reason of 'unreviewed' fails the scan: replace it (or route the call) before release.
# Regenerate missing entries with scripts/private_storage_scan.py --regen; prune stale
# ones with --prune. Sorted by path so merges are trivial.
TelegramConcierge/CLI/AdaMain.swift :: let child = Process() :: tty-handoff probe: /bin/sh reads the terminal and echoes a line, creates no files
TelegramConcierge/CLI/AdaMain.swift :: let rc = posix_spawn(&pid, spawnExe, nil, &attr, cargs, cenv) :: setsid trampoline fallback: spawns the target (or itself as __gate-exec in gate mode) with the umask already applied above (BRIGLIA_CHILD_UMASK), writes nothing itself
TelegramConcierge/CLI/AdaMain.swift :: try FileManager.default.createDirectory(at: tempRoot, withIntermediateDirectories: true) :: __gate-tty-selftest scratch under NSTemporaryDirectory (a journal for one probe job), removed at exit; test-only entry point
TelegramConcierge/CLI/AdaMain.swift :: try? "\(pid)".write(toFile: path, atomically: true, encoding: .utf8) :: outside the roots: the parent chooses the path via BRIGLIA_SETSID_PGID_FILE (UserdataToolchain uses NSTemporaryDirectory); a pid side channel, not state
TelegramConcierge/CLI/InstanceLock.swift :: let fd = open(url.path, O_CREAT | O_RDWR, 0o600) :: fd-based flock lock file with its own O_CREAT semantics; created 0600 (verified literal), lives in the 0700 data root
TelegramConcierge/CLI/MigrationEngine.swift :: guard rename(originalPath, dest.path) == 0 else { :: migration engine transaction: renames roots/parked assets it owns with journaled preimages; modes captured and restored exactly (H7)
TelegramConcierge/CLI/MigrationEngine.swift :: guard rename(root.new, root.old) == 0 else { :: migration engine transaction: renames roots/parked assets it owns with journaled preimages; modes captured and restored exactly (H7)
TelegramConcierge/CLI/MigrationEngine.swift :: guard rename(root.old, root.new) == 0 else { :: migration engine transaction: renames roots/parked assets it owns with journaled preimages; modes captured and restored exactly (H7)
TelegramConcierge/CLI/MigrationEngine.swift :: guard rename(stored.path, parked.originalPath) == 0 else { :: migration engine transaction: renames roots/parked assets it owns with journaled preimages; modes captured and restored exactly (H7)
TelegramConcierge/CLI/MigrationEngine.swift :: guard rename(tmp.path, url.path) == 0 else { :: migration engine transaction: renames roots/parked assets it owns with journaled preimages; modes captured and restored exactly (H7)
TelegramConcierge/CLI/MigrationEngine.swift :: let fd = open(tmp.path, O_WRONLY | O_CREAT | O_EXCL | O_CLOEXEC, 0o600) :: MigrationEngine.writeDurable: O_EXCL 0600 temp, explicit mode, checked fsync barriers — an equivalent-or-stronger private writer for the journal under ~/.local/state (outside the roots)
TelegramConcierge/CLI/MigrationEngine.swift :: let lockFD = open(lockPath, O_WRONLY | O_CREAT, 0o600) :: fd-based flock lock beside the migration journal under ~/.local/state (outside the roots); created 0600 (verified literal)
TelegramConcierge/CLI/MigrationEngine.swift :: let outFD = open(outURL.path, O_RDWR | O_CREAT | O_EXCL | O_CLOEXEC, 0o600) :: subprocess output capture: O_EXCL 0600 file under the 0700 journal area (~/.local/state, outside the roots) or NSTemporaryDirectory; unlinked after use
TelegramConcierge/CLI/MigrationEngine.swift :: let rc = posix_spawn(&pid, exe, &fileActions, &attr, cargs, cenv) :: migration engine: systemctl at a fixed path with output files created O_EXCL under the 0700 journal area (H7.5)
TelegramConcierge/CLI/MigrationEngine.swift :: try fm.createDirectory(at: dir, withIntermediateDirectories: true) :: MigrationEngine.writeDurable parent for the journal under ~/.local/state (outside the roots); the journal area itself is created 0700 and re-tightened by tightenJournalArea()
TelegramConcierge/CLI/MigrationEngine.swift :: try fm.createDirectory(at: parkedDir, withIntermediateDirectories: true, :: migration journal child under ~/.local/state (outside the roots); created with explicit 0700 attributes and re-tightened on recovery
TelegramConcierge/CLI/MigrationEngine.swift :: try fm.createDirectory(at: preimagesDir, withIntermediateDirectories: true, :: migration journal child under ~/.local/state (outside the roots); created with explicit 0700 attributes and re-tightened on recovery
TelegramConcierge/CLI/MigrationEngine.swift :: try fm.createDirectory(at: stateDirURL, withIntermediateDirectories: true, :: migration journal under ~/.local/state (outside the roots); created with explicit 0700 attributes and re-tightened on recovery
TelegramConcierge/CLI/MigrationEngine.swift :: try fm.createDirectory(atPath: parent, withIntermediateDirectories: true) :: outside the roots: the parent of a root being moved (~/.config, ~/.local/share, ~/Documents) is not ours to tighten; the roots themselves keep their captured modes
TelegramConcierge/CLI/MigrationEngine.swift :: try fm.createSymbolicLink(atPath: location, withDestinationPath: target) :: migration engine: the `ada` compatibility symlink in ~/.local/bin and parked-asset restore, outside both roots
TelegramConcierge/CLI/MigrationEngine.swift :: try fm.createSymbolicLink(atPath: spec.oldBinary, :: migration engine: the `ada` compatibility symlink in ~/.local/bin and parked-asset restore, outside both roots
TelegramConcierge/CLI/MigrationEngine.swift :: try? FileManager.default.createDirectory( :: parent of the migration lock under ~/.local/state/briglia-migrate, outside both roots; the lock itself is opened O_CREAT 0o600 on the next line
TelegramConcierge/CLI/MigrationEngine.swift :: try? fm.createDirectory(at: outputDir, withIntermediateDirectories: true, :: subprocess output area: the 0700 journal dir under ~/.local/state (outside the roots), created with explicit 0700 attributes
TelegramConcierge/CLI/QuickSetup/QuickSetupCommand.swift :: let fd = open(lockFileURL.path, O_CREAT | O_RDWR | O_NOFOLLOW | O_CLOEXEC, 0o600) :: quicksetup.lock: fd-based flock file with its own O_CREAT|O_NOFOLLOW semantics; created 0600 (verified literal) in the 0700 data root; holds pid/port/hint only
TelegramConcierge/CLI/QuickSetup/QuickSetupCommand.swift :: let p = Process() :: browser opener (xdg-open / $BROWSER) with the launch URL as its only argument; creates no files
TelegramConcierge/CLI/QuickSetup/QuickSetupWorkflow.swift :: env.enableService = { _ in fm.createFile(atPath: serviceMarker, contents: Data()); return nil } :: dev-build-only stub (BRIGLIA_DEV_QUICKSETUP_STUBS): an empty marker file in the 0700 data root standing in for the started service; never in release builds
TelegramConcierge/CLI/QuickSetup/QuickSetupWorkflow.swift :: try FileManager.default.createDirectory(atPath: unitDir, withIntermediateDirectories: true) :: outside the roots: ~/.config/systemd/user for the user unit (same as AgentServiceSupport.installUserService); the unit itself is written through PrivateStorage.writeAtomically mode 0644
TelegramConcierge/CLI/ReleaseSigning.swift :: FileManager.default.createFile(atPath: file.path, contents: nil) :: download target chosen by the caller: UpgradeService stages it under NSTemporaryDirectory (outside the roots); the selftest uses its own temp dir
TelegramConcierge/CLI/ReleaseSigning.swift :: let fd = open(lockURL.path, O_RDWR | O_CREAT | O_CLOEXEC, 0o600) :: fd-based flock sidecar lock (release_trust.json.lock) with its own O_CREAT semantics; created 0600 (verified literal)
TelegramConcierge/CLI/ReleaseSigning.swift :: try data.write(to: URL(fileURLWithPath: outputPath)) :: outside the roots: -dev-only __test-sign-envelope writes an envelope to a user-chosen --output path
TelegramConcierge/CLI/ServiceCommand.swift :: .write(toFile: unitPath, atomically: true, encoding: .utf8) :: outside the roots: systemd user unit under ~/.config/systemd/user (must stay readable to systemd --user)
TelegramConcierge/CLI/ServiceCommand.swift :: fm.createFile(atPath: root + "/sys/power/wake_lock", contents: Data()) :: service selftest fixture: fabricated sysfs node under NSTemporaryDirectory (outside the roots)
TelegramConcierge/CLI/ServiceCommand.swift :: fm.createFile(atPath: root + "/sys/power/wake_unlock", contents: Data()) :: service selftest fixture: fabricated sysfs node under NSTemporaryDirectory (outside the roots)
TelegramConcierge/CLI/ServiceCommand.swift :: let process = Process() :: launchctl/systemctl invocations: manage units, create nothing under the roots
TelegramConcierge/CLI/ServiceCommand.swift :: try FileManager.default.createDirectory( :: ~/.config/systemd/user is systemd's directory, outside both roots (unit files are not harness state)
TelegramConcierge/CLI/ServiceCommand.swift :: try fm.createDirectory(atPath: root + "/android", withIntermediateDirectories: true) :: service selftest fixture: fabricated root under NSTemporaryDirectory (outside the roots)
TelegramConcierge/CLI/ServiceCommand.swift :: try fm.createDirectory(atPath: root + "/etc", withIntermediateDirectories: true) :: service selftest fixture: fabricated root under NSTemporaryDirectory (outside the roots)
TelegramConcierge/CLI/ServiceCommand.swift :: try fm.createDirectory(atPath: root + "/etc/system-image", withIntermediateDirectories: true) :: service selftest fixture: fabricated root under NSTemporaryDirectory (outside the roots)
TelegramConcierge/CLI/ServiceCommand.swift :: try fm.createDirectory(atPath: root + "/sys/power", withIntermediateDirectories: true) :: service selftest fixture: fabricated root under NSTemporaryDirectory (outside the roots)
TelegramConcierge/CLI/ServiceCommand.swift :: try wakelockUnitText().write(to: temp, atomically: true, encoding: .utf8) :: outside the roots: unit text staged under NSTemporaryDirectory, then installed system-wide via sudo
TelegramConcierge/CLI/TriggerCommand.swift :: try Data("this is not a fire record".utf8).write(to: corruptURL) :: trigger selftest fixture: plants a deliberately corrupt record in the throwaway spool (XDG_DATA_HOME under NSTemporaryDirectory) to exercise quarantine
TelegramConcierge/CLI/TriggerCommand.swift :: try FileManager.default.createDirectory(at: tempRoot, withIntermediateDirectories: true) :: trigger/watcher selftest scratch root under NSTemporaryDirectory (outside the real roots)
TelegramConcierge/CLI/UpgradeService.swift :: if movedBinaryAside { try? fm.moveItem(at: oldBinary, to: executable) } :: upgrade transaction: swaps binary/bundle inside the install directory (the executable's own directory, ~/.local/bin) using aside names — installed software outside both roots, modes are the release tarball's
TelegramConcierge/CLI/UpgradeService.swift :: if movedBundleAside { try? fm.moveItem(at: oldBundle, to: installedBundle) } :: upgrade transaction: swaps binary/bundle inside the install directory (the executable's own directory, ~/.local/bin) using aside names — installed software outside both roots, modes are the release tarball's
TelegramConcierge/CLI/UpgradeService.swift :: let process = Process() :: runs the new binary for a version/bundle probe; it creates nothing
TelegramConcierge/CLI/UpgradeService.swift :: try fm.copyItem(at: newBinary, to: stagedBinary) :: upgrade staging: binary/bundle copied within ~/.local (outside both roots)
TelegramConcierge/CLI/UpgradeService.swift :: try fm.copyItem(at: newBundle, to: stagedBundle) :: upgrade staging: binary/bundle copied within ~/.local (outside both roots)
TelegramConcierge/CLI/UpgradeService.swift :: try fm.createDirectory(at: tmp, withIntermediateDirectories: true) :: outside the roots: upgrade staging dir under NSTemporaryDirectory, removed after install
TelegramConcierge/CLI/UpgradeService.swift :: try fm.moveItem(at: executable, to: oldBinary) :: upgrade transaction: swaps binary/bundle inside the install directory (the executable's own directory, ~/.local/bin) using aside names — installed software outside both roots, modes are the release tarball's
TelegramConcierge/CLI/UpgradeService.swift :: try fm.moveItem(at: installedBundle, to: oldBundle) :: upgrade transaction: swaps binary/bundle inside the install directory (the executable's own directory, ~/.local/bin) using aside names — installed software outside both roots, modes are the release tarball's
TelegramConcierge/CLI/UpgradeService.swift :: try fm.moveItem(at: stagedBinary, to: executable) :: upgrade transaction: swaps binary/bundle inside the install directory (the executable's own directory, ~/.local/bin) using aside names — installed software outside both roots, modes are the release tarball's
TelegramConcierge/CLI/UpgradeService.swift :: try fm.moveItem(at: stagedBundle, to: installedBundle) :: upgrade transaction: swaps binary/bundle inside the install directory (the executable's own directory, ~/.local/bin) using aside names — installed software outside both roots, modes are the release tarball's
TelegramConcierge/Services/AgentMailInstaller.swift :: guard rename(stagedBinary.path, versionedPath) == 0 else { :: AgentMail transaction: publishes the staged binary (0755, an executable other tools run) into ~/.local/bin, outside the roots; one atomic rename after the durable metadata write
TelegramConcierge/Services/AgentMailInstaller.swift :: let fd = open(lockURL.path, O_CREAT | O_RDWR | O_NOFOLLOW | O_CLOEXEC, 0o600) :: .agentmail.lock: fd-based installer flock with its own O_CREAT|O_NOFOLLOW semantics under ~/.local/bin (outside the roots); created 0600 (verified literal), refuses a non-regular file
TelegramConcierge/Services/AgentMailInstaller.swift :: try FileManager.default.createDirectory(at: installDirectory, withIntermediateDirectories: true) :: outside the roots: ~/.local/bin is the user's own bin directory (0755 by convention, shared with the briglia binary); deliberately NOT tightened to 0700
TelegramConcierge/Services/AgentMailInstaller.swift :: try fixture.archive.write(to: archivePath) :: AgentMail staging directory (~/.local/bin/.agentmail-staging-<uuid>, created 0700 below, outside the roots): the downloaded archive, removed with the staging directory
TelegramConcierge/Services/AgentMailInstaller.swift :: try fm.createDirectory(at: installDirectory, withIntermediateDirectories: true) :: outside the roots: ~/.local/bin is the user's own bin directory (0755 by convention, shared with the briglia binary); deliberately NOT tightened to 0700
TelegramConcierge/Services/AgentMailInstaller.swift :: try fm.createDirectory(at: staging, withIntermediateDirectories: false, attributes: [.posixPermissions: 0o700]) :: AgentMail staging directory under ~/.local/bin (outside the roots), created with explicit 0700 attributes; removed on success, failure and by repair
TelegramConcierge/Services/AgentMailInstaller.swift :: try wrapperBytes.write(to: stagedWrapper) :: AgentMail staging (0700 directory, outside the roots): the staged wrapper script, later published 0755 through PrivateStorage.writeAtomically with an explicit mode
TelegramConcierge/Services/BashTools.swift :: let p = Process() :: the agent's own shell commands: they act on the user's files with the user's umask by design (a work product must stay shareable); anything they leave under the roots is tightened by the next start's sweep, and every harness writer routes through PrivateStorage
TelegramConcierge/Services/BashTools.swift :: let process = Process() :: the agent's own shell commands: they act on the user's files with the user's umask by design (a work product must stay shareable); anything they leave under the roots is tightened by the next start's sweep, and every harness writer routes through PrivateStorage
TelegramConcierge/Services/ConversationManager.swift :: let destFD = open(destination.path, O_WRONLY | O_CREAT | O_EXCL, 0o600) :: fd-based O_EXCL name reservation for app-chat attachments (documents/ under the data root), created 0600 (verified literal) and streamed through the descriptor
TelegramConcierge/Services/ConversationManager.swift :: try FileManager.default.copyItem(at: url, to: privateCopy) :: attachment copied to a private temp copy under NSTemporaryDirectory for processing, removed after
TelegramConcierge/Services/DiffUtil.swift :: let proc = Process() :: diff(1) reads its two temp inputs and writes nothing
TelegramConcierge/Services/DiffUtil.swift :: try (new.data(using: .utf8) ?? Data()).write(to: newURL, options: .atomic) :: diff(1) input pair under NSTemporaryDirectory, outside both roots, removed on return
TelegramConcierge/Services/DiffUtil.swift :: try (old.data(using: .utf8) ?? Data()).write(to: oldURL, options: .atomic) :: diff(1) input pair under NSTemporaryDirectory, outside both roots, removed on return
TelegramConcierge/Services/DiscoveryTools.swift :: let process = Process() :: rg/find search processes: read-only
TelegramConcierge/Services/FileDescriptionService.swift :: try data.write(to: url, options: .atomic) :: selftest-only seam (_testStoreURL, a temp file the selftest supplies); production stores descriptions in UserDefaults, not in a file
TelegramConcierge/Services/FireOutbox.swift :: try FileManager.default.moveItem(at: fileURL, to: dest) :: quarantine move of a 0600 record within the 0700 fire-outbox directory; the mode travels with the file
TelegramConcierge/Services/GitCheckpoint.swift :: let process = Process() :: git stash create in the user's repository (the user's work product), nothing under the roots
TelegramConcierge/Services/GoogleWorkspaceService.swift :: let process = Process() :: gws CLI calls: read the API, write their own ~/.config/gws (outside both roots)
TelegramConcierge/Services/GoogleWorkspaceService.swift :: try data.write(to: clientSecretFileURL, options: [.atomic]) :: ~/.config/gws/client_secret.json belongs to the gws CLI, outside both roots; chmod 0600 explicitly on the next line
TelegramConcierge/Services/GoogleWorkspaceService.swift :: try fm.createDirectory(at: destDir, withIntermediateDirectories: true) :: ~/.local/bin is outside both roots (user-level executables, not harness state)
TelegramConcierge/Services/GoogleWorkspaceService.swift :: try fm.createDirectory(at: gwsConfigDirectory, withIntermediateDirectories: true) :: ~/.config/gws is the gws CLI's own directory, outside both roots (the config root is ~/.config/briglia)
TelegramConcierge/Services/GoogleWorkspaceService.swift :: try fm.createDirectory(at: tmpDir, withIntermediateDirectories: true) :: gws release tarball staged under NSTemporaryDirectory, outside both roots, removed after extraction
TelegramConcierge/Services/GoogleWorkspaceService.swift :: try fm.moveItem(at: extracted, to: dest) :: gws binary moved into ~/.local/bin, outside both roots
TelegramConcierge/Services/GoogleWorkspaceService.swift :: try tarData.write(to: tarPath) :: gws release tarball staged under NSTemporaryDirectory, outside both roots, removed after extraction
TelegramConcierge/Services/LSPClient.swift :: let proc = Process() :: language server: writes its caches/logs where it chooses (project dirs, ~/.cache), not under the roots; inherits the daemon umask
TelegramConcierge/Services/LSPLanguages.swift :: let process = Process() :: language-server availability probe (`--version`), creates nothing
TelegramConcierge/Services/LandingZone.swift :: try fm.createDirectory(at: directory(for: kind), withIntermediateDirectories: true) :: ~/Documents/Briglia is the user-browsed landing zone, deliberately outside the XDG roots (files for the user, not harness state)
TelegramConcierge/Services/LandingZone.swift :: try fm.createDirectory(at: root, withIntermediateDirectories: true) :: ~/Documents/Briglia is the user-browsed landing zone, deliberately outside the XDG roots (files for the user, not harness state)
TelegramConcierge/Services/LandingZone.swift :: try fm.createDirectory(at: scratchReposRoot, withIntermediateDirectories: true) :: ~/Documents/Briglia is the user-browsed landing zone, deliberately outside the XDG roots (files for the user, not harness state)
TelegramConcierge/Services/MCPAgentRouting.swift :: let fd = open(url.path, O_RDWR | O_CREAT | O_CLOEXEC, 0o600) :: routing sidecar flock file created O_CREAT 0o600 (literal); a lock file must keep its inode, so it cannot be replaced atomically
TelegramConcierge/Services/MCPClient.swift :: let proc = Process() :: MCP server process: writes where it chooses (Release C H4 governs its environment); the roots' state is written only by the harness
TelegramConcierge/Services/MCPRegistry.swift :: try fm.createDirectory(at: binDir, withIntermediateDirectories: true) :: ~/.local/bin is outside both roots (user-level executables, not harness state)
TelegramConcierge/Services/MCPRegistry.swift :: try fm.createDirectory(at: tmpDir, withIntermediateDirectories: true) :: node tarball staged under NSTemporaryDirectory, outside both roots, removed after extraction
TelegramConcierge/Services/MCPRegistry.swift :: try fm.createSymbolicLink(at: link, withDestinationURL: installRoot.appendingPathComponent("bin/\(tool)")) :: node/npm symlinks into ~/.local/bin, outside both roots
TelegramConcierge/Services/MCPRegistry.swift :: try fm.moveItem(at: extractedRoot, to: installRoot) :: node runtime installed under MCPRegistry.installRoot (outside both roots — see its definition)
TelegramConcierge/Services/MCPRegistry.swift :: try tarData.write(to: tarPath) :: node tarball staged under NSTemporaryDirectory, outside both roots, removed after extraction
TelegramConcierge/Services/ManagedPlaywright.swift :: guard rename(dir.path, corrupt.path) == 0 else { :: quarantine of a Playwright install that failed verification (rename to playwright.corrupt-<uuid>/ under the data root, modes unchanged, parent fsynced after) — callers guarantee the configuration no longer references it; removed at the next start
TelegramConcierge/Services/ManagedPlaywright.swift :: guard rename(staging.path, versionDir.path) == 0 else { :: publish of the staged Playwright tree to its immutable playwright-<hash>/ name under the data root: every file inside was created 0600/0700 by npm under umask 077 and the tree was fsynced bottom-up first; rename is the atomic commit, followed by the parent-directory fsync
TelegramConcierge/Services/ManagedPlaywright.swift :: let fd = open(url.path, O_RDWR | O_CREAT | O_CLOEXEC, 0o600) :: playwright-install.lock flock file created O_CREAT 0o600 (literal); a lock file must keep its inode, so it cannot be replaced atomically
TelegramConcierge/Services/ManagedPlaywright.swift :: let process = Process() :: npm ci (and the node --version probe) through the setsid trampoline with BRIGLIA_CHILD_UMASK=077: everything npm creates under the data root's mcp/ staging directory is owner-only from creation; the probe creates nothing
TelegramConcierge/Services/ManagedPlaywright.swift :: let renameResult = parkRenameOverride?(staging.path, manual.path) ?? rename(staging.path, manual.path) :: parks a staging tree whose process group could not be confirmed gone under playwright.manual-<uuid>/ (same data-root mcp/ directory, modes unchanged, parent fsync checked after); no automatic path ever touches that name, bootstraps refuse until a human removes it; the override is a selftest seam that simulates a failed rename
TelegramConcierge/Services/MindExportService.swift :: destination.path.withCString { dest in rename(source, dest) } :: export: atomic replace of the user-chosen destination archive (outside both roots)
TelegramConcierge/Services/MindExportService.swift :: let process = Process() :: zip/unzip: export writes the archive in temp then at the user-chosen destination; import extracts into temp, then the policy-aware restore copies into the roots
TelegramConcierge/Services/MindExportService.swift :: try FileManager.default.copyItem(at: source, to: destination) :: export staging: current state copied into the temp assembly directory (outside both roots) for zipping
TelegramConcierge/Services/MindExportService.swift :: try configData.write(to: tempDir.appendingPathComponent("mind_config.json")) :: outside the roots: export assembly dir under NSTemporaryDirectory; the finished archive goes to the user-chosen destination
TelegramConcierge/Services/MindExportService.swift :: try fm.copyItem(at: tempZip, to: sibling) :: export: finished archive copied next to the user-chosen destination (outside both roots)
TelegramConcierge/Services/MindExportService.swift :: try fm.createDirectory(at: tempDir, withIntermediateDirectories: true) :: outside the roots: export assembly / import staging dir under NSTemporaryDirectory
TelegramConcierge/Services/PermissionsService.swift :: let process = Process() :: pmset/systemctl read-only probes
TelegramConcierge/Services/ProjectsZipAutoExtractor.swift :: let process = Process() :: unzip into projects/ (excluded from the policy: the user's work product)
TelegramConcierge/Services/ProjectsZipAutoExtractor.swift :: try? fileManager.createDirectory(at: folder, withIntermediateDirectories: true) :: projects/ under the data root is excluded from the policy (the user's work product, plan H2.2)
TelegramConcierge/Services/QuickSetupEvidence.swift :: let process = Process() :: silent evidence probe runner (pip --version, pmset, systemctl, gsettings): read-only commands with piped output; creates no files
TelegramConcierge/Services/ReminderService.swift :: try (try encoder.encode(rows)).write(to: file, options: .atomic) :: outside the roots: rewrites reminders.json inside the staged Mind import tree under NSTemporaryDirectory; the copy into the data root happens later via MindExportService
TelegramConcierge/Services/SessionAffinity.swift :: guard rename(fileURL.path, candidate.path) == 0 else { :: affinity quarantine: renames the undecodable owner-only affinity.json beside itself (same 0700 data root) under affinity.lock, then fsyncs the directory (plan §5)
TelegramConcierge/Services/SessionAffinity.swift :: guard rename(tmp.path, fileURL.path) == 0 else { :: affinity write: atomic rename of the 0600 sibling temp file onto affinity.json under affinity.lock, followed by a checked directory fsync (plan §5)
TelegramConcierge/Services/SessionAffinity.swift :: let fd = open(lockURL.path, O_RDWR | O_CREAT | O_NOFOLLOW | O_CLOEXEC, 0o600) :: affinity.lock: fd-based flock with its own O_CREAT|O_NOFOLLOW semantics in the 0700 data root; created 0600 (verified literal)
TelegramConcierge/Services/SessionAffinity.swift :: let fd = open(tmp.path, O_WRONLY | O_CREAT | O_EXCL | O_CLOEXEC, 0o600) :: affinity write: exclusive 0600 sibling temp file in the 0700 data root, fsynced then renamed (same contract as PrivateStorage.replaceContents, with the test seams the plan requires)
TelegramConcierge/Services/SetupJobRunner.swift :: let rc = posix_spawn(&pid, selfPath, &actions, &attr, cargs, cenv) :: quick-setup job gate: spawns this binary's __setsid-exec/__gate-exec trampoline with the gate pipes dup2'd; the package managers it releases write to their own system locations, and the journal is written through PrivateStorage.writeAtomically
TelegramConcierge/Services/StreamingOutput.swift :: guard FileManager.default.createFile( :: streaming spill file inside the 0700 briglia-tool-output/ dir under NSTemporaryDirectory, outside both roots; created 0600 (attributes on the next lines), swept after 7 days
TelegramConcierge/Services/StreamingOutput.swift :: try FileManager.default.createDirectory(atPath: dir, withIntermediateDirectories: true, attributes: [.posixPermissions: 0o700]) :: briglia-tool-output/ spill dir under NSTemporaryDirectory, outside both roots, created 0700; the spill file itself is created 0600 below
TelegramConcierge/Services/TelegramBotService.swift :: try data.write(to: localURL) :: outside the roots: voice-note download to NSTemporaryDirectory, deleted after transcription
TelegramConcierge/Services/ToolExecutor.swift :: let didCreateOutputFile = FileManager.default.createFile( :: shortcut output capture file under NSTemporaryDirectory for the shortcuts CLI to write into, outside both roots, removed after the run
TelegramConcierge/Services/ToolExecutor.swift :: let process = Process() :: media/shortcut helper processes (ffmpeg, whisper, convert, shortcuts): write temp files or caller-chosen output paths, not harness state
TelegramConcierge/Services/ToolExecutor.swift :: try FileManager.default.createDirectory( :: parent of a caller-chosen output path (inspect_media save_to, SRT next to the input media), not harness state
TelegramConcierge/Services/ToolExecutor.swift :: try input.write(to: inputFile!, atomically: true, encoding: .utf8) :: shortcut input staged under NSTemporaryDirectory for the shortcuts CLI, outside both roots
TelegramConcierge/Services/ToolExecutor.swift :: try output.write(to: saveURL, atomically: true, encoding: .utf8) :: inspect_media save_to is a caller-chosen path (the OCR transcript goes where the agent asked), not harness state
TelegramConcierge/Services/ToolExecutor.swift :: try srt.write(to: outputURL, atomically: true, encoding: .utf8) :: SRT destination is a caller-chosen path (next to the input media or an explicit output_path), not harness state
TelegramConcierge/Services/ToolchainService.swift :: let process = Process() :: apt/brew/dpkg toolchain install: system package manager, nothing under the roots
TelegramConcierge/Services/TriggerSpool.swift :: O_WRONLY | O_CREAT, 0o600) :: fd-based flock spool lock (.spool.lock) with its own O_CREAT semantics; created 0600 (verified literal) inside the 0700 trigger-events dir
TelegramConcierge/Services/TriggerSpool.swift :: try FileManager.default.moveItem(at: tempURL, to: finalURL) :: spool record: 0600 temp (written by PrivateStorage) renamed to its final name inside the 0700 spool directory
TelegramConcierge/Services/TruncationService.swift :: try fm.createDirectory(atPath: truncationDir, withIntermediateDirectories: true, :: briglia-tool-output/ spill dir under NSTemporaryDirectory, outside both roots, created 0700 (attributes on the next line)
TelegramConcierge/Services/TruncationService.swift :: try text.write(toFile: path, atomically: true, encoding: .utf8) :: truncated-output spill file inside the 0700 briglia-tool-output/ dir under NSTemporaryDirectory, outside both roots; swept after 7 days
TelegramConcierge/Services/UserdataToolchain.swift :: .write(to: baseStatus, atomically: true, encoding: .utf8) :: toolchain/ is excluded (apt state under the toolchain root, owned by the transactional installer)
TelegramConcierge/Services/UserdataToolchain.swift :: .write(to: txTmp.appendingPathComponent("manifest.json"), :: toolchain/ is excluded (transaction snapshot under the toolchain root, owned by the transactional installer)
TelegramConcierge/Services/UserdataToolchain.swift :: .write(to: url, atomically: true, encoding: .utf8) :: toolchain/ is excluded (LibreOffice rc files inside the extracted prefix)
TelegramConcierge/Services/UserdataToolchain.swift :: .write(to: wrapper, atomically: true, encoding: .utf8) :: outside the roots: installs a 0755 wrapper into ~/.local/bin (or BRIGLIA_TOOLCHAIN_BIN)
TelegramConcierge/Services/UserdataToolchain.swift :: do { try fm.moveItem(at: backupDir, to: prefixDir) } catch { :: toolchain/ prefix/staging/backup swaps — excluded area owned by the transactional installer
TelegramConcierge/Services/UserdataToolchain.swift :: do { try fm.moveItem(at: prefixDir, to: backupDir) } catch { :: toolchain/ prefix/staging/backup swaps — excluded area owned by the transactional installer
TelegramConcierge/Services/UserdataToolchain.swift :: do { try fm.moveItem(at: stagingDir, to: prefixDir) } catch { :: toolchain/ prefix/staging/backup swaps — excluded area owned by the transactional installer
TelegramConcierge/Services/UserdataToolchain.swift :: do { try text.write(to: url, atomically: true, encoding: .utf8) } :: toolchain/ is excluded (LibreOffice rc relocation inside the extracted prefix)
TelegramConcierge/Services/UserdataToolchain.swift :: guard rename(tmp.path, url.path) == 0 else { :: toolchain/ is excluded (owned by the transactional installer); its own durable writer
TelegramConcierge/Services/UserdataToolchain.swift :: guard rename(txTmp.path, txDir.path) == 0 else { :: toolchain/ is excluded (owned by the transactional installer); its own durable writer
TelegramConcierge/Services/UserdataToolchain.swift :: let fd = open(path, O_WRONLY | O_CREAT, 0o600) :: fd-based flock lock (toolchain.lock) beside the toolchain root with its own O_CREAT semantics; created 0600 (verified literal)
TelegramConcierge/Services/UserdataToolchain.swift :: let process = Process() :: apt-get/dpkg-deb extraction into toolchain/ (excluded) through the setsid trampoline
TelegramConcierge/Services/UserdataToolchain.swift :: try "briglia toolchain conversion probe".write(to: txt, atomically: true, :: toolchain/ is excluded (conversion probe under the toolchain root, removed after the probe)
TelegramConcierge/Services/UserdataToolchain.swift :: try data.write(to: tmp) :: UserdataToolchain.writeDurable: temp + checked fsync + rename + directory fsync for state under toolchain/, which is excluded
TelegramConcierge/Services/UserdataToolchain.swift :: try fm.createDirectory(at: dir, withIntermediateDirectories: true) :: UserdataToolchain.writeDurable parent under toolchain/, which is excluded
TelegramConcierge/Services/UserdataToolchain.swift :: try fm.createDirectory(at: txTmp.appendingPathComponent("wrappers"), :: toolchain/ is excluded (transaction snapshot under the toolchain root)
TelegramConcierge/Services/UserdataToolchain.swift :: try fm.moveItem(at: backupDir, to: prefixDir) :: toolchain/ prefix/staging/backup swaps — excluded area owned by the transactional installer
TelegramConcierge/Services/UserdataToolchain.swift :: try text.write(to: wrapper, atomically: true, encoding: .utf8) :: outside the roots: restores a 0755 wrapper into ~/.local/bin (or BRIGLIA_TOOLCHAIN_BIN)
TelegramConcierge/Services/UserdataToolchain.swift :: try? fm.createDirectory(at: dir, withIntermediateDirectories: true) :: apt/build/probe dirs under toolchain/ (excluded) plus the ~/.local/bin wrapper dir (outside the roots)
TelegramConcierge/Services/WebOrchestrator+Helpers.swift :: try? fm.moveItem(at: current, to: previous) :: log rotation within logs/: the file is already 0600 (openForAppend tightened it) and keeps its mode
TelegramConcierge/Services/WhatsAppChannelService.swift :: let proc = Process() :: node bridge / npm install through the setsid trampoline with BRIGLIA_CHILD_UMASK=077: everything they create under the data root is owner-only from creation
TelegramConcierge/Utilities/KeychainHelper.swift :: let fd = open(lockURL.path, O_CREAT | O_WRONLY, 0o600) :: secret-store sidecar flock file created O_CREAT 0o600 (literal); a lock file must keep its inode, so it cannot be replaced atomically
TelegramConcierge/Utilities/PlatformCompat.swift :: let process = Process() :: platform probe (uname/lsb), read-only
TelegramConcierge/Utilities/PlatformMedia.swift :: do { try FileManager.default.copyItem(at: url, to: fileURL) } catch { :: media work copy under NSTemporaryDirectory, removed after processing
TelegramConcierge/Utilities/PlatformMedia.swift :: do { try FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true) } catch { return nil } :: convert(1) work dir under NSTemporaryDirectory, outside both roots, removed on return
TelegramConcierge/Utilities/PlatformMedia.swift :: do { try data.write(to: fileURL) } catch { :: PDF work copy in an ada-pdf- dir under NSTemporaryDirectory, outside both roots, removed in deinit
TelegramConcierge/Utilities/PlatformMedia.swift :: do { try data.write(to: src) } catch { return nil } :: convert(1) input under NSTemporaryDirectory, outside both roots, removed on return
TelegramConcierge/Utilities/PlatformMedia.swift :: do { try data.write(to: tmp) } catch { return nil } :: identify(1) input under NSTemporaryDirectory, outside both roots, removed on return
TelegramConcierge/Utilities/PlatformMedia.swift :: try FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true) :: ada-pdf- work dir under NSTemporaryDirectory, outside both roots, removed in deinit
TelegramConcierge/Utilities/PlatformMedia.swift :: try? FileManager.default.createDirectory(at: rasterDir, withIntermediateDirectories: true) :: pdftoppm raster dir inside the ada-pdf- work dir under NSTemporaryDirectory, outside both roots, removed on return
TelegramConcierge/Utilities/PlatformMedia.swift :: try? FileManager.default.createDirectory(at: sliceDir, withIntermediateDirectories: true) :: pdfseparate slice dir inside the ada-pdf- work dir under NSTemporaryDirectory, outside both roots, removed on return
TelegramConcierge/Utilities/PrivateStorage+FileTools.swift :: try FileManager.default.createDirectory(at: base.deletingLastPathComponent(), withIntermediateDirectories: true) :: out-of-scope branch of the file-tool policy: parent directory of the user's own file, created as before (no mode policy outside the roots)
TelegramConcierge/Utilities/PrivateStorage+FileTools.swift :: try data.write(to: URL(fileURLWithPath: path), options: .atomic) :: out-of-scope branch of the file-tool policy (apply_patch add/move to a destination the plan verified absent): the user's own file keeps the pre-H2 atomic replace
TelegramConcierge/Utilities/PrivateStorage+FileTools.swift :: try data.write(to: URL(fileURLWithPath: path).resolvingSymlinksInPath(), options: .atomic) :: out-of-scope branch of the file-tool policy (apply_patch rollback): the user's own file keeps the pre-H2 behaviour — resolved link, atomic replace
TelegramConcierge/Utilities/PrivateStorage+FileTools.swift :: try data.write(to: targetURL, options: .atomic) :: out-of-scope branch of the file-tool policy: the user's own file (outside the roots or in projects/ / toolchain/) keeps the pre-H2 behaviour — resolved link, atomic replace, exact previous mode restored
TelegramConcierge/Utilities/PrivateStorage+Restore.swift :: guard rename(tmp.path, destURL.path) == 0 else { :: PrivateStorage's own streaming copier: atomic rename of the finished 0600/owner-mode temp
TelegramConcierge/Utilities/PrivateStorage+Restore.swift :: guard symlink(target, destination.path) == 0 else { :: policy-aware restore: recreates an archived symlink only where the destination is not harness-owned state (checked on the lines above)
TelegramConcierge/Utilities/PrivateStorage+Restore.swift :: let dst = open(tmp.path, O_WRONLY | O_CREAT | O_EXCL | O_CLOEXEC, 0o600) :: PrivateStorage's own streaming copier: exclusive 0600 temp in the destination directory, final mode before rename
TelegramConcierge/Utilities/PrivateStorage+Writers.swift :: try FileManager.default.createDirectory(at: url, withIntermediateDirectories: true) :: the outside-scope branch of ensureDirectoryScoped: paths classified .outside (external locations, projects/, toolchain/) are created under the umask on purpose
TelegramConcierge/Services/SubscriptionAuth.swift :: let fd = open(lockFile.path, O_RDWR | O_CREAT | O_NOFOLLOW | O_CLOEXEC | O_NONBLOCK, 0o600) :: Dedicated auth sidecar; explicit 0600, owner/type/link-count validation, CLOEXEC and cancellable flock; never recreated or unlinked by production

TelegramConcierge/Services/ResponsesUsage.swift :: let fd = open(lockFile.path, O_RDWR | O_CREAT | O_NOFOLLOW | O_CLOEXEC | O_NONBLOCK, 0o600) :: Bounded diagnostic sidecar lock, no-follow regular owned single-link 0600 validation; ledger writes use PrivateStorage.
TelegramConcierge/Services/ResponsesUsage.swift :: try FileManager.default.moveItem(at: file, to: parked) :: Corrupt diagnostic quarantine: checked owner-only regular single-link file, inode revalidated, unique sibling destination under the sidecar lock; preserves original bytes/mode and checks directory fsync before starting a new ledger.
